VITE_TOMTOM_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — enables the natural-language explainer in the Behaviour Prediction
# panel (one Claude call per scenario run, never per agent). Leave blank to use
# the built-in deterministic, model-derived explanation instead.
VITE_ANTHROPIC_API_KEY=
VITE_ANTHROPIC_MODEL=
